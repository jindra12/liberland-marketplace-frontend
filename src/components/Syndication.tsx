import * as React from "react";
import { SyndicationList } from "./lists/SyndicationList";

const Syndication: React.FunctionComponent = () => {
    return <SyndicationList />;
};

export default Syndication;
