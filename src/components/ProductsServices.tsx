import * as React from "react";
import { ProductsServicesList } from "./lists/ProductServiceList";

const ProductsServices: React.FunctionComponent = () => {
    return <ProductsServicesList />;
};

export default ProductsServices;
