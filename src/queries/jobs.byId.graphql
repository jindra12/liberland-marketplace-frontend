query JobById($id: String!) {
  Job(id: $id) {
    id
    title
    description
    location
    employmentType
    postedAt
    applyUrl
    salaryRange {
      min
      max
      currency
    }
    company {
      id
      name
      image {
        id
        url
        alt
        filename
        width
        height
        mimeType
      }
    }
    image {
      id
      url
      alt
      filename
      width
      height
      mimeType
    }
  }
}
