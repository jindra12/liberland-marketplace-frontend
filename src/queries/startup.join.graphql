mutation JoinStartup($id: String!) {
    joinStartup(id: $id) {
        message
        startup {
            id
            title
            involvedUsers {
                id
                name
                email
            }
        }
    }
}
