mutation CreateStartup($data: mutationStartupInput!, $draft: Boolean!) {
    createStartup(data: $data, draft: $draft) {
        id
        title
        _status
        image {
            id
            url
        }
    }
}
