mutation SubscribeToProductUpdates($targetID: String!) {
    createNotificationSubscription(data: { targetCollection: products, targetID: $targetID }) {
        id
        targetCollection
        targetID
    }
}
