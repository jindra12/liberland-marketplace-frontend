query CompanyById($id: String!) {
  Company(id: $id) {
    id
    name
    _status
    description
    website
    phone
    email
    allowedIdentities {
      id
      name
      description
    }
    disallowedIdentities {
      id
      name
      description
    }
    createdBy {
      id
    }
    identity {
      id
      name
      description
      website
      image {
        id
        url
        alt
        filename
        width
        height
        mimeType
      }
    }
    image {
      id
      url
      alt
      filename
      width
      height
      mimeType
    }
  }
}
