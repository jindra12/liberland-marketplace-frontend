mutation SubscribeToCompanyUpdates($email: String!, $targetID: String!) {
    createNotificationSubscription(data: { email: $email, targetCollection: companies, targetID: $targetID }) {
        id
        email
        targetCollection
        targetID
    }
}
