mutation SubscribeToCompanyUpdates($targetID: String!) {
    createNotificationSubscription(data: { targetCollection: companies, targetID: $targetID }) {
        id
        targetCollection
        targetID
    }
}
