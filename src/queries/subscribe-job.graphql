mutation SubscribeToJobUpdates($targetID: String!) {
    createNotificationSubscription(data: { targetCollection: jobs, targetID: $targetID }) {
        id
        targetCollection
        targetID
    }
}
