mutation SubscribeToJobUpdates($email: String!, $targetID: String!) {
    createNotificationSubscription(data: { email: $email, targetCollection: jobs, targetID: $targetID }) {
        id
        email
        targetCollection
        targetID
    }
}
