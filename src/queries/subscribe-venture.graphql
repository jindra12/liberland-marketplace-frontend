mutation SubscribeToVentureUpdates($email: String!, $targetID: String!) {
    createNotificationSubscription(data: { email: $email, targetCollection: startups, targetID: $targetID }) {
        id
        email
        targetCollection
        targetID
    }
}
