query ListJobs($page: Int = 1, $limit: Int = 20, $sort: String) {
  Jobs(page: $page, limit: $limit, sort: $sort) {
    docs {
      id
      title
      description
      company {
        id
        name
      }
    }
    totalDocs
    limit
    totalPages
    page
    hasPrevPage
    hasNextPage
    prevPage
    nextPage
  }
}
