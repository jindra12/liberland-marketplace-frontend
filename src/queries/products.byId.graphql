query ProductById($id: String!) {
  Product(id: $id, draft: false) {
    id
    name
    description
    url
    company {
      id
      name
      description
      image {
        id
        url
        alt
        filename
        width
        height
        mimeType
      }
    }
    image {
      id
      url
      alt
      filename
      width
      height
      mimeType
    }
  }
}
