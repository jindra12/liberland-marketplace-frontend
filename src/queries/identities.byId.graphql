query IdentityById($id: String!) {
  Identity(id: $id, draft: false) {
    id
    name
    description
    website
    image {
      id
      url
      alt
      filename
      width
      height
      mimeType
    }
  }
}
