mutation CreateProduct($data: mutationProductInput!, $draft: Boolean!) {
  createProduct(data: $data, draft: $draft) {
    id
    name
    cryptoAddresses {
      ethereum
      tron
      solana
    }
    _status
    properties {
      id
      key
      value
    }
    image {
      id
      url
    }
  }
}
