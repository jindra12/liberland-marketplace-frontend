mutation SubscribeToTribeUpdates($targetID: String!) {
    createNotificationSubscription(data: { targetCollection: identities, targetID: $targetID }) {
        id
        targetCollection
        targetID
    }
}
